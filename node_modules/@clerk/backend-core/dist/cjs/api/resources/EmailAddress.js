"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.EmailAddress = void 0;
const IdentificationLink_1 = require("./IdentificationLink");
const Verification_1 = require("./Verification");
class EmailAddress {
    constructor(id, emailAddress, verification, linkedTo) {
        this.id = id;
        this.emailAddress = emailAddress;
        this.verification = verification;
        this.linkedTo = linkedTo;
    }
    static fromJSON(data) {
        return new EmailAddress(data.id, data.email_address, data.verification && Verification_1.Verification.fromJSON(data.verification), data.linked_to.map(link => IdentificationLink_1.IdentificationLink.fromJSON(link)));
    }
}
exports.EmailAddress = EmailAddress;
