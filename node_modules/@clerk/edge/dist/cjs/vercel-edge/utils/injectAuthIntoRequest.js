"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.injectAuthIntoRequest = void 0;
function injectAuthIntoRequest(req, authData) {
    const { user, session, userId, sessionId, getToken, claims } = authData;
    const auth = {
        userId,
        sessionId,
        getToken,
        claims,
    };
    return Object.assign(req, { auth, user, session });
}
exports.injectAuthIntoRequest = injectAuthIntoRequest;
