"use strict";
exports.__esModule = true;
exports.injectAuthIntoRequest = void 0;
/**
 * Inject `auth`, `user` and `session` properties into ctx.request
 * @internal
 */
function injectAuthIntoRequest(ctx, authData) {
    var user = authData.user, session = authData.session, userId = authData.userId, sessionId = authData.sessionId, getToken = authData.getToken, claims = authData.claims, organization = authData.organization;
    ctx.req.auth = { userId: userId, sessionId: sessionId, getToken: getToken, claims: claims };
    ctx.req.user = user;
    ctx.req.session = session;
    ctx.req.organization = organization;
    return ctx;
}
exports.injectAuthIntoRequest = injectAuthIntoRequest;
//# sourceMappingURL=injectAuthIntoRequest.js.map